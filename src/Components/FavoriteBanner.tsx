import { Typography } from "@material-ui/core";
import React from "react";

export default function FavoriteBanner(){
    return <div className={"banner"}>
    <Typography className={"bannerItem"} variant="h5">
      Explore Your Favorite Asteroid 
    </Typography>

  </div>
}